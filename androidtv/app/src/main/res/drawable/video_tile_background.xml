<?xml version="1.0" encoding="utf-8"?>
<!--
    An outline alone reads as decoration from a sofa. The focused tile also
    sits on a filled panel, so which one is selected is obvious at a glance
    even to someone not looking for a border.
-->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/focus_fill" />
            <corners android:radius="16dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="16dp" />
        </shape>
    </item>
</selector>
