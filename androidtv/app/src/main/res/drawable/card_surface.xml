<?xml version="1.0" encoding="utf-8"?>
<!-- Cartão dentro de uma gaveta: um degrau acima do fundo dela, com contorno.
     É a camada que não existia e por isso nada tinha relevo. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/surface_raised" />
    <stroke android:width="@dimen/hairline_width" android:color="@color/outline" />
    <corners android:radius="@dimen/radius_lg" />
</shape>
